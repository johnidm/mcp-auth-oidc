# Keycloak Configuration Example
# Copy this to .env to use Keycloak as your OAuth provider

# Keycloak server URL (no trailing slash)
KEYCLOAK_SERVER_URL=http://localhost:8080

# Keycloak realm name
KEYCLOAK_REALM=mcp-demo

# Client credentials (get these from Keycloak Admin Console)
KEYCLOAK_CLIENT_ID=mcp-auth-demo
KEYCLOAK_CLIENT_SECRET=your-client-secret-here

# Server Configuration
RESOURCE_ID=http://localhost:8000
SERVER_HOST=0.0.0.0
SERVER_PORT=8000

# Optional: Choose auth provider (auth0 or keycloak)
AUTH_PROVIDER=keycloak

# =============================================================================
# Quick Start with Docker
# =============================================================================
# 1. Start Keycloak: docker-compose up -d
# 2. Access admin console: http://localhost:8080 (admin/admin)
# 3. Get client secret from: Clients → mcp-auth-demo → Credentials
# 4. Update KEYCLOAK_CLIENT_SECRET above
# 5. Copy this file: cp keycloak.env .env
# 6. Start server: python run.py
#
# Demo User Credentials:
# - Email: demo@example.com
# - Password: demo123
# =============================================================================

